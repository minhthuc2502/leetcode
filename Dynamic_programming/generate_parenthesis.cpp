#include <vector>
#include <string>

using namespace std;

class Solution {
public:
    vector<string> generateParenthesis(int n) {
        // n = 2 (()) ()()
        // ()()() (())() ((())) ()(()) (()())
        vector<string> result;
    }
};

int main() {
    return 0;
}